import React from "react";
// import PropTypes from 'prop-types';
import { Avatar as Ui5Avatar } from '@ui5/webcomponents-react/lib/Avatar';


const Avatar = (props) => {
    return (
        <Ui5Avatar {...props} />
    );
};

// Avatar.defaultProps = {
// };

// Avatar.propTypes = {
// };

export default Avatar;
